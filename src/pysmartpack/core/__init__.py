"""Core domain layer: scanning, environment detection, config generation,
packaging execution and the optional LLM advisor.

This package is pure Python and has **no dependency on the UI layer**, so it can
be unit-tested and reused headlessly (CLI, CI, etc.).
"""
